{# nsswitch.conf.j2 - Name service switch configuration for DC-as-client. #}
{# winbind resolves AD users/groups on the DC itself. #}
passwd:         files winbind
group:          files winbind
shadow:         files

hosts:          files dns
networks:       files

protocols:      db files
services:       db files
ethers:         db files
rpc:            db files
