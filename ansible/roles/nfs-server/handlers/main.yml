# nfs-server handlers - NFS-server-specific service handlers (none).
#
# All NFS-server handlers (Reload NFS exports, Restart nfs-idmapd,
# Restart rpc-gssd, Restart nfs-mountd, Restart rpc-svcgssd, Reload systemd)
# now live in the common role's handlers/main.yml and are loaded via the
# meta/main.yml dependency.  This file is kept as an explicit marker so
# anyone adding nfs-server-specific handlers in the future knows where they
# go.
---
[]
