# samba-mgmt.conf - Site-specific configuration for the Samba AD DC management scripts.
#
# Format: KEY=VALUE (no quotes required unless the value contains spaces).
# Lines starting with # are comments.  This file is read by lib/config.sh
# and the values are exported as environment variables.  On a deployed DC
# this file is generated from
# ansible/roles/samba-dc/templates/config/samba-mgmt.conf.j2; this in-repo
# copy is the fallback for local syntax checks and ad-hoc invocations.
#
# SECURITY: Do NOT store passwords here.  All admin/user passwords are
# prompted interactively by the management scripts.
#
# --- Domain identity ---
REALM=EXAMPLE.INTERNAL
DOMAIN=EXAMPLE
NETBIOS=EXAMPLE

# --- Domain controller ---
DC_HOSTNAME=dc01

# --- Samba paths ---
SAMBA_CONF=/etc/samba/smb.conf
SHARE_BASE=/data
HOME_BASE=/home

# --- User defaults ---
DEFAULT_SHELL=/bin/bash
DEFAULT_GROUP="Domain Users"

# --- Logging ---
LOG_FILE=/var/log/samba-management.log

# --- Linux client automount ---
AUTOMOUNT_BASE=/mnt/shares

# --- NFS defaults baked into autofs entries created by samba-automount.sh.
#     NFS_SERVER is the host clients should mount shares from (DC or a
#     dedicated storage host).  NFS_HOMES_SERVER is where user home
#     directories live; samba-user.sh SSHes here as root when it isn't
#     this host.  Both default to the local FQDN when unset.
NFS_SEC=krb5p
NFS_SERVER=
NFS_HOMES_SERVER=

# DNS forwarder, NTP servers, and TLS settings live in the Ansible role
# defaults (samba_dns_forwarder / samba_ntp_servers / samba_tls_enabled) --
# the bin/* scripts don't consume them, so they're omitted here.
