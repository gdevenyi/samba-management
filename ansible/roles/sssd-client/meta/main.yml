---
galaxy_info:
  author: samba-management contributors
  description: Join a Linux host to a Samba AD domain via SSSD/realmd.
  license: MIT
  min_ansible_version: "2.14"

# Pull in the shared handlers (Reload systemd, Restart sssd, NFS service
# handlers) so notify: works without duplicating them in this role.
dependencies:
  - role: common
